看完了。当前分支 `restart/from-ori` 的 `HEAD` 和 `origin/restart/from-ori` 一样，都是 `099a851 change freq sort`，所以相比上次 push 没有新增 commit，只有工作区未提交改动：

- [src/ic3/IC3.ml](/home/lyh/kind2-2.3.0-OrderIC3-v2/src/ic3/IC3.ml:435)：新增了一套识别“单变量线性不等式方向”的逻辑。
  - 能解析简单线性表达式里的 `+`、`-`、常数乘法、数值常量、state var。
  - 能判断一个 literal 是 `LowerBound` 还是 `UpperBound`，并处理顶层 `not (...)`。
  - 新增 `same_direction_single_bound` 表，用来记录同一个单变量 key 下的 bound 方向。
  - 核心行为变化在 [src/ic3/IC3.ml](/home/lyh/kind2-2.3.0-OrderIC3-v2/src/ic3/IC3.ml:630)：以前同一个单变量 key 出现两次就给 `cluster = 2` 结构保护；现在如果这两个都是同方向 bound，例如两个上界或两个下界，就降回 `cluster = 1`，不再保护这个 pair。

- [FREQ_SORT_STRATEGY.md](/home/lyh/kind2-2.3.0-OrderIC3-v2/FREQ_SORT_STRATEGY.md:54)：文档同步补充了这个 guardrail。
  - 明确 `x <= c1` 和 `x <= c2`、`x >= c1` 和 `x >= c2` 这类同方向 shifted/sliding bounds 不再作为结构 pair 延后删除。
  - 说明这个规则只会 demote 回 `cluster = 1`，不会单独触发排序。
  - 还补了 `DRAGON_8_e7_3752`、`car_5_e7_244` 的 benchmark 经验说明。

改动规模是：

```text
FREQ_SORT_STRATEGY.md |  52 +++++++++++++++++-
src/ic3/IC3.ml        | 148 ++++++++++++++++++++++++++++++++++++++++++++++++--
2 files changed, 194 insertions(+), 6 deletions(-)
```

我也验证了一下构建：`dune build src/kind2.exe` 通过。直接跑 `dune build` 会被仓库现有问题挡住：缺 `ounit2`，以及 `scratch/template_key_coarse_test.ml` 里引用了不存在的 `Clause.template_key_coarse`，不像是这次改动造成的。



你说得对，不能凭嘴说。看完这一轮 DRAGON/car 簇后，我的结论是：

**不要用 “same-direction sliding bound pair” 本身判断重要性。这个信号不可靠。**

我扫了整个 `20260524_170939_freq_v2` 里的 DRAGON/car `ic3qe_freq_sort` 日志，结果很明显：

```text
car_5_e7_244_e1_823   win   same_dir=11
car_5                 win   same_dir=33
car_5_e7_244          loss  same_dir=22

DRAGON_8_e7_3752      timeout same_dir=5
DRAGON_8_e3_786...    huge win same_dir=4
DRAGON_4_e2_2799      loss same_dir=31
```

所以 “看到 same-direction bound pair 就删/就留” 都不科学。

更可靠的区分是这个：

**坏的 DRAGON 形态是：单变量 bound 被保留下来以后，和含同一个变量的多变量 affine bound 组成递推链。**

比如 `DRAGON_8_e7_3752` 里，核心坏形态在这里：

[DRAGON_8_e7_3752.log:2254](/home/lyh/kind2-exp/OrderIC3_Auto/runs/20260524_170939_freq_v2/logs/ic3qe_freq_sort/FMCAD08/Int/memory1/DRAGON_8_e7_3752.log:2254)

```text
call_32_2 <= 0
call_32_2 <= 1
call_32_2 + call_32_3 >= 2
call_32_2 + call_32_3 <= 1
```

排序后保护了两个 `call_32_2` 单变量 bound：

[DRAGON_8_e7_3752.log:2263](/home/lyh/kind2-exp/OrderIC3_Auto/runs/20260524_170939_freq_v2/logs/ic3qe_freq_sort/FMCAD08/Int/memory1/DRAGON_8_e7_3752.log:2263)

最后学出：

[DRAGON_8_e7_3752.log:2321](/home/lyh/kind2-exp/OrderIC3_Auto/runs/20260524_170939_freq_v2/logs/ic3qe_freq_sort/FMCAD08/Int/memory1/DRAGON_8_e7_3752.log:2321)

```text
call_32_2 <= 1
call_32_2 + call_32_3 >= 2
```

这就是坏的 sliding chain：单变量 bound + overlapping affine bound，常数往前滑。

而 car 的有用形态不是这个。比如 `car_5_e7_244_e1_823` 早期是：

[car_5_e7_244_e1_823.log:414](/home/lyh/kind2-exp/OrderIC3_Auto/runs/20260524_170939_freq_v2/logs/ic3qe_freq_sort/FMCAD08/Int/simulation/car_5_e7_244_e1_823.log:414)

```text
not (= call_2_3 7)
not (> -call_2_4 - 1 0)
```

最后保留的是一个单变量 bound：

[car_5_e7_244_e1_823.log:455](/home/lyh/kind2-exp/OrderIC3_Auto/runs/20260524_170939_freq_v2/logs/ic3qe_freq_sort/FMCAD08/Int/simulation/car_5_e7_244_e1_823.log:455)

```text
not (> -call_2_4 - 1 0)
```

这不是 affine sliding chain，而是 phase/equality guard 旁边留下来的边界切面。

所以我现在认为可靠办法应该是：

```text
重要性 = 删除试探反馈 + 上下文形态
```

具体规则雏形：

1. **在线记录删除成败**
   - 如果某个 normalized literal 删除成功，说明它不重要，下次同类先删。
   - 如果删除失败，说明它重要，下次同类后删。
   - 这个比 frequency 靠谱，因为 frequency 只是“活下来了几次”，不等于“不可删”。

2. **初始先验不要看 same-direction pair，而看上下文**
   - `single bound + overlapping multi-var affine bound + no equality/mode guard`：倾向先删。
   - `single bound + equality/disequality/mode guard`：倾向后删或至少中性。
   - 如果同一 affine template 的常数在最近 learned clauses 里连续推进，比如 `c, c+1, c+2`，强烈判为坏 sliding chain，先删。

3. **不要保护整个 pair，只给每个 literal 打重要性分**
   - `call_32_2 <= 1` 这种如果总是和 `call_32_2 + call_32_3 >= 2` 一起留下，降分。
   - `call_2_4 >= 1` 这种如果删除经常失败并形成短 clause，升分。

一句话：**不是识别“这种子句类型重要不重要”，而是识别“这个 bound 在当前 proof 里是 phase guard 的边界，还是 affine sliding chain 的燃料”。**  
DRAGON 坏的是后者；car 里有用的是前者。


我的实现思路会分两步，不急着大改。

第一步：先把现有排序改成“显式角色评分”。

现在代码里其实已经有这些隐含信号：

```text
structural_support_of  -> 像边界
same_direction_pair    -> 不像边界 / 可能是养料
boundary_delay         -> 不等式像边界
frequency              -> 历史保留下来的 lit
```

我会包一层：

```ocaml
type role_score = {
  fuel : int;
  boundary : int;
}
```

然后为每个 literal 算：

```text
boundary:
  +1 structural cluster
  +1 inequality boundary_delay
  +1 和 equality/disequality guard 同 clause
  +1 和 boolean/mode guard 同 clause
  +1 删除失败次数高

fuel:
  +1 same-direction single-bound pair
  +2 single bound 与 overlapping affine bound 同 clause
  +2 最近出现 sliding sequence
  +1 删除成功次数高
```

排序时用：

```text
role_priority = fuel - boundary
```

`role_priority` 越大，越早删；越小，越晚删。

第二步：加在线反馈，不只靠静态形状。

在 `linear_search` 里其实天然知道删除结果：

```ocaml
drop_literal ()
keep_literal ()
```

所以可以维护两个表：

```ocaml
lit_drop_success : normalized_lit -> float
lit_drop_fail    : normalized_lit -> float
```

含义：

```text
drop 成功：
  说明这个 lit 不是必须的，下次同类 fuel +1

drop 失败：
  说明这个 lit 当前像边界，下次同类 boundary +1
```

注意这里最好用 normalized key，不要直接用原始 term。否则 `x <= 1`、`x <= 2` 学不到同一类经验。至少先按：

```text
变量集合 + 比较方向 + 单变量/affine 类型
```

第三步：识别 sliding chain。

这个不是一开始就做复杂证明，只做一个局部启发式：

```text
如果当前 clause 里有：
  single bound: x <= c
  affine bound: x + y >= c+k

并且最近 learned clauses 里出现过同 template 的：
  x <= c-1
  x + y >= c-1+k
```

那就认为它是 sliding fuel：

```text
fuel += 2
```

这个能专门打 `DRAGON_8_e2_3896_e3_3125` 这种：

```text
call_32_2 <= 0, call_32_2 + call_32_3 >= 1
call_32_2 <= 1, call_32_2 + call_32_3 >= 2
call_32_2 <= 2, call_32_2 + call_32_3 >= 3
```

但不会轻易伤 `car_5_e7_244_e1_823`，因为 car 的好 bound 是：

```text
call_2_4 >= 1
```

旁边是 equality/mode guard，不是 `call_2_4 + y` 的 affine sliding。

最终排序我会改成类似：

```text
expensive_arithmetic_rank
role_priority
structural_support
boundary_delay
frequency
original_index
```

或者更保守一点：

```text
expensive_arithmetic_rank
fuel_score
structural_support
-boundary_score
boundary_delay
frequency
original_index
```

我倾向第一种，因为日志更好解释：

```text
fuel=2 boundary=0 role=2   -> 养料，早删
fuel=0 boundary=2 role=-2  -> 边界，晚删
```

最重要的是日志要打出来：

```text
[exp=0 cluster=1 bdelay=1 freq=1.990 fuel=2 boundary=0 role=2]
```

这样每次看 DRAGON/car 就能直接知道：排序器到底把它当成了养料，还是边界。